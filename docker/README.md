# Docker Setup

This folder belongs to Docker documentation and local container guidance.

Use the root-level `docker-compose.yml` to start both services:

```bash
make docker-up
```

The frontend will be available on `http://localhost:5173` and the backend on `http://localhost:8000`.
